const fruits = ['apple', 'banana', 'orange', 'mango'];
const myList = fruits.map((item) => item.valueOf()); // no idea how to get a value
console.log(myList)
console.log(fruits)


const makeDouble = [1, 2, 3, 4, 5]

const demoList = makeDouble.map((val) => val*2)
console.log(demoList) // [ 2, 4, 6, 8, 10 ]